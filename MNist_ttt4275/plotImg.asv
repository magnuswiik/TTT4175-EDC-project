function img = plotImg(flat_img, prediction, label, correct)
img = zeros(28,28);
img(:) = flat_img(:);
figure(1)
image(img);
text = ""Prediction: " + num2str(prediction) + " Label: " + num2str(label);
title(text);
end

